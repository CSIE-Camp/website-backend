version: '3'

services:
  server:
    build: .
    ports: 
      - "8080:8080"
  db:
    image: postgres:latest
    restart: unless-stopped
    container_name: db
    env_file:
      - .env
    expose:
      - 5432:5432
    volumes:
      - db-data:/test

volumes:
  db-data: